import { ICreateUserRole, IUserRole } from "../../types/user-roles";

export class UserRoleModel{
    // static async create(userRoleData: ICreateUserRole): Promise<IUserRole>{
    //     const {designation} = userRoleData;
    //     const result = await query(`INSERT INTO user-roles (designation) VALUES ($1)`)
    // }
}